name: fistikpazar
description: "Yeni bir Flutter projesi."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.2.2
  mailjet: ^1.0.4+4
  sexy_api_client: ^0.0.6+6
  http: ^1.2.1
  flutter_svg: ^2.0.9
  data_filters: ^1.0.2
  image_picker: ^1.1.2
  curved_navigation_bar: ^1.0.3
  fl_chart: ^0.68.0
  provider: ^6.1.2
  intl: ^0.19.0
  flutter_rating_bar: ^4.0.1
  carousel_slider: ^4.2.1
  dio: ^5.4.3+1
  flutter_localizations:
    sdk: flutter
  collection: ^1.18.0
  http_parser: ^4.0.2
  url_launcher: ^6.3.0
  google_maps_flutter: ^2.7.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - images/
    - icons/

  # Yerelleştirmeler
  generate: true
