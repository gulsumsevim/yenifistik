import 'package:flutter/material.dart';

class ProductStatisticsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Ürün İstatistikleri'),
      ),
      body: Center(
        child: Text('Ürün İstatistikleri sayfası'),
      ),
    );
  }
}
